import type { Metadata } from "next"; import { db } from "@/lib/db"; import { StaffCard } from "@/components/cards"; import { EmptyState } from "@/components/ui";
export const metadata:Metadata={title:"الكادر الوظيفي"};export default async function StaffPage(){const groups=await db.staffGroup.findMany({orderBy:{displayOrder:"asc"},include:{members:{where:{active:true},orderBy:{displayOrder:"asc"}}}});const hasMembers=groups.some(group=>group.members.length);return <><div className="page-hero"><div className="container"><h1>الكادر الوظيفي</h1></div></div><section className="page-section"><div className="container">{hasMembers?<div className="staff-groups">{groups.map((group,index)=>{if(!group.name&&index>0&&!groups[index-1].name)return null;const unnamedRun=!group.name?groups.slice(index,(()=>{const next=groups.findIndex((item,itemIndex)=>itemIndex>index&&Boolean(item.name));return next===-1?groups.length:next})()).flatMap(item=>item.members):group.members;return <section key={group.id} className="staff-group">{group.name&&<h2 className="staff-group-title">{group.name}</h2>}<div className="grid-3">{unnamedRun.map(member=><StaffCard key={member.id} member={member}/>)}</div></section>})}</div>:<EmptyState title="سيتم تحديث الفريق قريبًا" text="نعود إليك بمعلومات فريقنا."/>}</div></section></>}
