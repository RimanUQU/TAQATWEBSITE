import Link from "next/link";import { db } from "@/lib/db";import { deletePartnerAction,savePartnerAction } from "@/actions/admin";import { ActiveToggle,AdminHeader,TextField } from "@/components/admin-ui";import { UploadField } from "@/components/upload-field";import { Button } from "@/components/ui";
export default async function Partners({searchParams}:{searchParams:Promise<{edit?:string}>}){const{edit}=await searchParams;const[items,current]=await Promise.all([db.partner.findMany({orderBy:{displayOrder:"asc"}}),edit?db.partner.findUnique({where:{id:edit}}):null]);return <><AdminHeader title="الشركاء"/><form action={savePartnerAction} className="panel admin-form"><input type="hidden" name="id" value={current?.id||""}/><TextField name="name" label="اسم الشريك" value={current?.name} required/><UploadField name="logo" label="شعار الشريك (اختياري)" value={current?.logo}/><TextField name="url" label="رابط الموقع" value={current?.url||""}/><TextField name="displayOrder" label="ترتيب العرض" type="number" value={current?.displayOrder??items.length+1}/><ActiveToggle checked={current?.active??true}/><Button>{current?"حفظ التعديل":"إضافة الشريك"}</Button>{current&&<Link className="btn btn-outline" href="/admin/partners">إلغاء</Link>}</form><section className="page-section"><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>الشريك</th><th>الرابط</th><th>الترتيب</th><th>الحالة</th><th>إجراء</th></tr></thead><tbody>{items.map(i=><tr key={i.id}><td>{i.name}</td><td>{i.url||"—"}</td><td>{i.displayOrder}</td><td>{i.active?"نشط":"مخفي"}</td><td className="table-actions"><Link className="btn btn-outline btn-sm" href={`/admin/partners?edit=${i.id}`}>تعديل</Link><form action={deletePartnerAction.bind(null,i.id)}><Button variant="text" size="sm">حذف</Button></form></td></tr>)}</tbody></table></div></section></>}
