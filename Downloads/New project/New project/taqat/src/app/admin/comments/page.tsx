import { db } from "@/lib/db";import { moderateCommentAction } from "@/actions/admin";import { AdminHeader } from "@/components/admin-ui";import { Badge,Button } from "@/components/ui";import { formatDate } from "@/lib/utils";
export default async function Comments({searchParams}:{searchParams:Promise<{status?:string}>}){const{status}=await searchParams;const filter=status&&["PENDING","APPROVED","HIDDEN"].includes(status)?status as "PENDING"|"APPROVED"|"HIDDEN":undefined;const items=await db.programComment.findMany({where:filter?{status:filter}:{},include:{user:true,program:true},orderBy:{createdAt:"desc"}});return <><AdminHeader title="تعليقات البرامج" actions={<div className="table-actions"><a className="btn btn-outline btn-sm" href="/admin/comments?status=PENDING">بانتظار المراجعة</a><a className="btn btn-outline btn-sm" href="/admin/comments?status=APPROVED">المعتمدة</a><a className="btn btn-outline btn-sm" href="/admin/comments?status=HIDDEN">المخفية</a></div>}/><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>المستخدمة</th><th>البرنامج</th><th>التعليق</th><th>التاريخ</th><th>الحالة</th><th>إجراءات</th></tr></thead><tbody>{items.map(i=><tr key={i.id}><td>{i.user.name}</td><td>{i.program.title}</td><td>{i.body}</td><td>{formatDate(i.createdAt)}</td><td><Badge>{i.status}</Badge></td><td className="table-actions"><form action={moderateCommentAction.bind(null,i.id,"APPROVED")}><Button size="sm" variant="secondary">اعتماد</Button></form><form action={moderateCommentAction.bind(null,i.id,"HIDDEN")}><Button size="sm" variant="outline">إخفاء</Button></form><form action={moderateCommentAction.bind(null,i.id,"DELETE")}><Button size="sm" variant="text">حذف</Button></form></td></tr>)}</tbody></table></div></>}
